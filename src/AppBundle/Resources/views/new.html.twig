{% extends 'base.html.twig' %}

{% block title %}New Book {% endblock %}
{% block body %}

    <h1 class="page-header text-center">
        Book
    </h1>

    <div class="container">
        <div class="row">
            <div class="col-sm-4"></div>
            <div class="col-sm-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {#{{ form_label(form.user) }}#}
                <div class="form-group">
                    <div class="col-sm-10">
                        <div class="dropzone"></div>
                    </div>
                </div>
                <div class="fixed-wrapper">
                    <input type="submit" class="btn btn-block btn-primary" value="Save" />
                </div>
                <div style="text-align: center">
                    <a href="{{ path('book_list') }}">Cancel</a>
                </div>

                {#{{ form_row(form.upload_book) }}#}
            {{ form_end(form) }}
            </div>
            <div class="col-sm-4"></div>
        </div>
    </div>
{% endblock body %}

{% block javascripts %}
    {{ parent() }}

    <script>

        Dropzone.autoDiscover = false;
        const dropzone_default = new Dropzone(".dropzone", {
            url: '{{ path('fileuploadhandler') }}' ,
            maxFiles: 1,
            dictMaxFilesExceeded: 'Only 1 Image can be uploaded',
            acceptedFiles: 'image/*, application/*',
            maxFilesize: 30,  // in Mb
            addRemoveLinks: true,
            init: function () {

//                var mockFile = { name: "5784b1ccf62c61f22f310c44dafdda90.jpeg", size: 12345, type: 'image/jpeg' };
//                this.addFile.call(this, mockFile);
//                this.options.thumbnail.call(this, mockFile, "http://http://localhost:8080/symfonyNew/web/app_dev.php/new/5784b1ccf62c61f22f310c44dafdda90.jpeg");

                this.on("maxfilesexceeded", function(file) {
                    this.removeFile(file);
                });
                this.on("sending", function(file, xhr, formData) {
                });
                this.on("success", function(file, response) {
                    if (response.uploaded)
                        alert('File Uploaded: ' + response.fileName);
                });

                $('#fileName').val(response.fileName);
                $('#linked_ID').val(response.mediaEntityId);

//                var mockFile = { name: "Filename", size: 30 };
//                Dropzone.emit("addedfile", mockFile);
//                Dropzone.emit("thumbnail", mockFile, "/image/url");
//                Dropzone.createThumbnailFromUrl(file, imageUrl, callback, crossOrigin);
//                Dropzone.emit("complete", mockFile);
//                var existingFileCount = 1;
//                Dropzone.options.maxFiles = Dropzone.options.maxFiles - existingFileCount;

            }
        });



    </script>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js"></script>
    <script src="{{ asset('bundles/starrating/js/rating.js') }}"></script>
{% endblock javascripts %}